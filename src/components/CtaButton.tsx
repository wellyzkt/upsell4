import React from 'react';

interface CtaButtonProps {
  text: string;
}

const CtaButton: React.FC<CtaButtonProps> = () => {
  return null; // Component disabled as we're using one-click buttons instead
};

export default CtaButton;